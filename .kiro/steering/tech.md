# Technology Stack

## Build System
- **Package Manager:** npm (package-lock.json present, lockfileVersion 3)
- Node.js-based project

## Tech Stack
Currently minimal setup with no dependencies defined yet.

## Common Commands
Since the project is in early stages, standard npm commands will apply:
- `npm install` - Install dependencies
- `npm run <script>` - Run package.json scripts (once defined)
- `npm test` - Run tests (once configured)

## Development Environment
- **IDE:** VSCode configuration present
- **MCP Configuration:** Disabled in workspace settings
